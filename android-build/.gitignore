# Android Build - Local gitignore
# This ensures sensitive files are never committed

# Build outputs
app/
build/
*.apk
*.aab

# Gradle files (will be generated by bubblewrap)
gradle/
gradlew
gradlew.bat
.gradle/
local.properties

# CRITICAL: Keystore must NEVER be committed!
android.keystore
*.keystore
*.jks

# Temporary files
*.tmp
*.bak
*~

# Keep these files in Git:
# - twa-manifest.json (app configuration)
# - README.md (instructions)
# - .gitignore (this file)

