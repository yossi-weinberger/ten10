# Localized Reminder Email Redesign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a branded, bilingual tithe reminder email with optional first-name greeting, localized balance states, Excel import guidance, and one deterministic monthly encouragement item.

**Architecture:** Keep the existing cron, service-role RPC, SES Raw MIME, consent, and unsubscribe pipeline. Extend the existing RPC to return `full_name` and normalized language, keep all reminder copy in one typed Edge Function module, render email-safe table HTML with a hosted blurred header asset and solid fallback, and generate HTML, subject, and plain text from the same template data.

**Tech Stack:** Supabase Postgres migration, Supabase Edge Functions on Deno, TypeScript, AWS SES v2 Raw MIME, Vitest 4, email-safe HTML tables and inline CSS.

## Global Constraints

- Supported languages are exactly `he` and `en`.
- Missing, empty, or unknown language values fall back to `he`.
- The source of truth remains `profiles.client_preferences.language`.
- Do not add `preferred_language` or dual-write language.
- Do not add i18next or React dependencies to the Edge Function.
- Keep imports at the top of every module.
- Use exhaustive handling for TypeScript unions.
- Preserve reminder scheduling, Shabbat handling, consent filtering, unsubscribe URLs, List-Unsubscribe headers, SES tags, and rate limiting.
- Use `Asia/Jerusalem` for monthly encouragement selection.
- Keep the email width at 600px and use presentation tables with inline styles.
- The approved Hebrew encouragement label is `פינת החיזוק`.
- The approved header is the soft teal and gold blurred treatment with a warm cream fallback.
- Do not apply a remote migration or deploy an Edge Function without explicit user approval.
- Do not create git commits unless the user explicitly requests them.

---

## File Structure

### Create

- `supabase/functions/send-reminder-emails/email-copy.ts`
  - Owns supported languages, localized strings, direction metadata, and the 12 monthly encouragement entries.
- `supabase/functions/send-reminder-emails/email-copy.test.ts`
  - Verifies locale completeness, fallback behavior, and month selection.
- `supabase/functions/send-reminder-emails/email-templates.test.ts`
  - Verifies subject, HTML, plain text, personalization, balance states, direction, escaping, header asset, and unsubscribe copy.
- `public/email/reminder-header-blur.png`
  - Approved pre-rendered teal and gold blur used behind the email logo.
- Migration generated by:
  - `npx supabase@latest migration new localize_reminder_email_context`
  - The CLI-generated file becomes the canonical definition of `get_reminder_users_with_emails(integer)`.

### Modify

- `vitest.config.ts`
  - Include pure Edge Function unit tests.
- `supabase/functions/send-reminder-emails/email-templates.ts`
  - Replace the current Hebrew-only template with pure localized subject, HTML, and text generators.
- `supabase/functions/send-reminder-emails/user-service.ts`
  - Carry normalized language and full name from the RPC to email delivery.
- `supabase/functions/send-reminder-emails/simple-email-service.ts`
  - Pass localization and personalization data and remove the hardcoded English plain text.
- `supabase/functions/send-reminder-emails/README.md`
  - Correct filenames, reminder days, color semantics, localization, and test instructions.
- `llm-instructions/features/email/email-reminders-feature-complete-guide.md`
  - Document the new design and localization path and remove stale implementation examples.
- `llm-instructions/features/email/email-system-future-improvements.md`
  - Remove the obsolete dedicated-language-column proposal and mark completed items.
- `llm-instructions/backend/supabase-integration-status.md`
  - Record the canonical RPC return fields and localized reminder behavior.

### Explicitly Unchanged

- `supabase/functions/send-reminder-emails/index.ts`
- `supabase/functions/send-reminder-emails/jwt-utils.ts`
- `supabase/functions/_shared/simple-email-service.ts`
- `src/lib/store.ts`
- `src/lib/services/preferences-sync.service.ts`
- `src/lib/i18n.ts`

---

### Task 1: Add Edge Function Unit Test Discovery

**Files:**
- Modify: `vitest.config.ts`
- Create: `supabase/functions/send-reminder-emails/email-copy.test.ts`

**Interfaces:**
- Consumes: existing Vitest configuration.
- Produces: Vitest discovery for `supabase/functions/**/*.test.ts`.

- [ ] **Step 1: Add one intentionally failing discovery test**

Create `supabase/functions/send-reminder-emails/email-copy.test.ts`:

```ts
import { describe, expect, it } from "vitest";

describe("reminder email copy", () => {
  it("is discovered by Vitest", () => {
    expect(true).toBe(false);
  });
});
```

- [ ] **Step 2: Expand Vitest include patterns**

Update `vitest.config.ts`:

```ts
test: {
  environment: "node",
  include: [
    "src/**/*.test.{ts,tsx}",
    "supabase/functions/**/*.test.ts",
  ],
},
```

- [ ] **Step 3: Verify the new test is discovered and fails**

Run:

```powershell
npm test -- supabase/functions/send-reminder-emails/email-copy.test.ts
```

Expected: one failed test with `expected true to be false`.

- [ ] **Step 4: Replace the discovery assertion with the locale contract imports**

Replace the test contents with:

```ts
import { describe, expect, it } from "vitest";
import {
  getMonthlyEncouragement,
  normalizeReminderLanguage,
  REMINDER_COPY,
} from "./email-copy.ts";

describe("reminder email copy", () => {
  it("supports exactly Hebrew and English", () => {
    expect(Object.keys(REMINDER_COPY).sort()).toEqual(["en", "he"]);
  });

  it("falls back to Hebrew for missing or unsupported values", () => {
    expect(normalizeReminderLanguage(undefined)).toBe("he");
    expect(normalizeReminderLanguage(null)).toBe("he");
    expect(normalizeReminderLanguage("")).toBe("he");
    expect(normalizeReminderLanguage("fr")).toBe("he");
  });

  it("keeps English when explicitly selected", () => {
    expect(normalizeReminderLanguage("en")).toBe("en");
  });

  it("contains exactly 12 encouragement entries per language", () => {
    expect(REMINDER_COPY.he.monthlyEncouragements).toHaveLength(12);
    expect(REMINDER_COPY.en.monthlyEncouragements).toHaveLength(12);
  });

  it("selects the same semantic month index in both languages", () => {
    for (let month = 1; month <= 12; month += 1) {
      expect(getMonthlyEncouragement("he", month)).toBe(
        REMINDER_COPY.he.monthlyEncouragements[month - 1],
      );
      expect(getMonthlyEncouragement("en", month)).toBe(
        REMINDER_COPY.en.monthlyEncouragements[month - 1],
      );
    }
  });

  it("rejects month numbers outside 1 through 12", () => {
    expect(() => getMonthlyEncouragement("he", 0)).toThrow(
      "Month must be between 1 and 12",
    );
    expect(() => getMonthlyEncouragement("he", 13)).toThrow(
      "Month must be between 1 and 12",
    );
  });
});
```

- [ ] **Step 5: Run the test and verify the missing module failure**

Run:

```powershell
npm test -- supabase/functions/send-reminder-emails/email-copy.test.ts
```

Expected: FAIL because `email-copy.ts` does not exist.

- [ ] **Step 6: Stop for review**

Review the test-only diff. Do not commit unless explicitly requested.

---

### Task 2: Create the Typed Bilingual Copy Registry

**Files:**
- Create: `supabase/functions/send-reminder-emails/email-copy.ts`
- Test: `supabase/functions/send-reminder-emails/email-copy.test.ts`

**Interfaces:**
- Produces:
  - `type ReminderLanguage = "he" | "en"`
  - `type TextDirection = "rtl" | "ltr"`
  - `interface MonthlyEncouragement`
  - `interface ReminderLocaleCopy`
  - `const REMINDER_COPY`
  - `normalizeReminderLanguage(value)`
  - `getMonthlyEncouragement(language, month)`

- [ ] **Step 1: Add the locale types and helper signatures**

Create `email-copy.ts` with:

```ts
export type ReminderLanguage = "he" | "en";
export type TextDirection = "rtl" | "ltr";

export interface MonthlyEncouragement {
  body: string;
  source?: string;
}

interface BalanceCopy {
  outstanding: string;
  credit: string;
  settled: string;
}

interface SubjectCopy {
  outstanding: (amount: string) => string;
  credit: (amount: string) => string;
  settled: string;
}

export interface ReminderLocaleCopy {
  htmlLang: ReminderLanguage;
  direction: TextDirection;
  locale: "he-IL" | "en-US";
  slogan: string;
  greeting: string;
  reminder: string;
  balance: BalanceCopy;
  verification: string;
  importHintPrefix: string;
  importHintEmphasis: string;
  cta: string;
  encouragementLabel: string;
  footerPreferenceNote: string;
  unsubscribeReminder: string;
  unsubscribeAll: string;
  subject: SubjectCopy;
  monthlyEncouragements: readonly MonthlyEncouragement[];
}
```

- [ ] **Step 2: Add the exact 12-entry Hebrew and English content**

Use these month-aligned entries:

```ts
const HEBREW_ENCOURAGEMENTS = [
  {
    body: "עשר תעשר את כל תבואת זרעך היוצא השדה שנה שנה",
    source: "דברים יד, כב",
  },
  {
    body: "עשר בשביל שתתעשר",
    source: "תלמוד בבלי, תענית ט׳ ע״א",
  },
  {
    body: "הביאו את כל המעשר אל בית האוצר... והריקותי לכם ברכה עד בלי די",
    source: "מלאכי ג, י",
  },
  {
    body: "וכל אשר תתן לי עשר אעשרנו לך",
    source: "בראשית כח, כב",
  },
  {
    body: "הפרשת מעשר מדויקת הופכת כוונה טובה להרגל של נתינה.",
  },
  {
    body: "גם כשחומש מלא קשה, אפשר להתחיל בהדרגה ולהוסיף בנתינה כשמתאפשר.",
    source: "על פי המקורות ההלכתיים של Ten10",
  },
  {
    body: "החפץ חיים הציע למפרישי חומש לחשב מעשר פעמיים, כדי לשמור על דיוק.",
    source: "אהבת חסד",
  },
  {
    body: "כספי המעשר נועדו לצדקה, לחסד ולמעשים טובים.",
    source: "מתוך ספריית ההלכה של Ten10",
  },
  {
    body: "יש שמייעצים לחלק את המעשר בין עזרה לעניים לבין מטרות מצווה חשובות.",
    source: "מתוך ספריית ההלכה של Ten10",
  },
  {
    body: "הלוואה לנזקק דרך גמ״ח יכולה להיות צדקה שממשיכה לעזור שוב ושוב.",
    source: "על פי אהבת חסד",
  },
  {
    body: "גם הנחה או ויתור לנזקק יכולים להפוך מסחר יומיומי למעשה של צדקה.",
    source: "מתוך ספריית ההלכה של Ten10",
  },
  {
    body: "Ten10 צמחה מטבלאות Excel פשוטות שנועדו לעזור לחשב מעשר וחומש בדיוק.",
    source: "סיפור הקמת Ten10",
  },
] as const satisfies readonly MonthlyEncouragement[];

const ENGLISH_ENCOURAGEMENTS = [
  {
    body: "You shall surely tithe all the produce of your planting, year by year.",
    source: "Deuteronomy 14:22",
  },
  {
    body: "Give ma'aser so that you will prosper.",
    source: "Babylonian Talmud, Taanit 9a",
  },
  {
    body: "Bring the full tithe into the storehouse... and I will pour out blessing without end.",
    source: "Malachi 3:10",
  },
  {
    body: "Of all that You give me, I will surely give a tenth to You.",
    source: "Genesis 28:22",
  },
  {
    body: "Accurate ma'aser turns a good intention into a lasting habit of giving.",
  },
  {
    body: "When giving a full chomesh is difficult, one can begin gradually and increase when possible.",
    source: "Based on Ten10's halachic library",
  },
  {
    body: "The Chafetz Chaim advised those giving chomesh to calculate ma'aser twice for greater accuracy.",
    source: "Ahavas Chesed",
  },
  {
    body: "Ma'aser funds are meant for tzedakah, kindness, and good deeds.",
    source: "From Ten10's halachic library",
  },
  {
    body: "Some authorities advise dividing ma'aser between helping the poor and other important mitzvah purposes.",
    source: "From Ten10's halachic library",
  },
  {
    body: "A loan through a gemach can become tzedakah that helps again and again.",
    source: "Based on Ahavas Chesed",
  },
  {
    body: "Even a discount or waived payment for someone in need can turn everyday commerce into tzedakah.",
    source: "From Ten10's halachic library",
  },
  {
    body: "Ten10 grew from simple Excel sheets created to help people calculate ma'aser and chomesh accurately.",
    source: "The Ten10 origin story",
  },
] as const satisfies readonly MonthlyEncouragement[];
```

Before production deployment, require one product and rabbinic editorial review of all 24 entries. Infrastructure implementation can proceed before that review.

- [ ] **Step 3: Add the exact locale registry**

```ts
export const REMINDER_COPY = {
  he: {
    htmlLang: "he",
    direction: "rtl",
    locale: "he-IL",
    slogan: "ניהול מעשרות ותקציב פיננסי פשוט ומדויק",
    greeting: "ערב טוב",
    reminder: "תזכורת קצרה לעדכון המעשרות, כדי לשמור על יתרה מדויקת.",
    balance: {
      outstanding: "יתרת המעשר שלך לתרומה היא",
      credit: "הינך נמצא בזכות של",
      settled: "יתרת המעשר שלך מאוזנת",
    },
    verification:
      "כדאי לוודא שהוספת את ההכנסות, ההוצאות והתרומות האחרונות.",
    importHintPrefix: "יש לך כמה פעולות לעדכן?",
    importHintEmphasis: "אפשר לייבא אותן בקלות מקובץ Excel.",
    cta: "לעדכון המעשרות ב־Ten10",
    encouragementLabel: "פינת החיזוק",
    footerPreferenceNote: "נשלח אליך לפי העדפות התזכורת שבחרת",
    unsubscribeReminder: "הפסקת תזכורות חודשיות",
    unsubscribeAll: "ביטול הרשמה מכל המיילים",
    subject: {
      outstanding: (amount) => `תזכורת מעשר - נותרו ${amount} לתרומה`,
      credit: (amount) => `תזכורת מעשר - הינך בזכות של ${amount}`,
      settled: "תזכורת מעשר - היתרה שלך מאוזנת",
    },
    monthlyEncouragements: HEBREW_ENCOURAGEMENTS,
  },
  en: {
    htmlLang: "en",
    direction: "ltr",
    locale: "en-US",
    slogan: "Simple and Accurate Tithe and Financial Budget Management",
    greeting: "Good evening",
    reminder:
      "A quick reminder to update your tithes and keep your balance accurate.",
    balance: {
      outstanding: "Your remaining tithe balance is",
      credit: "You currently have a credit of",
      settled: "Your tithe balance is settled",
    },
    verification:
      "Make sure you have added your latest income, expenses, and donations.",
    importHintPrefix: "Several transactions to update?",
    importHintEmphasis: "You can easily import them from an Excel file.",
    cta: "Update your tithes in Ten10",
    encouragementLabel: "Encouragement Corner",
    footerPreferenceNote:
      "Sent according to the reminder preferences you selected",
    unsubscribeReminder: "Stop monthly reminders",
    unsubscribeAll: "Unsubscribe from all emails",
    subject: {
      outstanding: (amount) => `Tithe reminder - ${amount} remaining`,
      credit: (amount) => `Tithe reminder - ${amount} credit`,
      settled: "Tithe reminder - your balance is settled",
    },
    monthlyEncouragements: ENGLISH_ENCOURAGEMENTS,
  },
} satisfies Record<ReminderLanguage, ReminderLocaleCopy>;
```

- [ ] **Step 4: Add normalization and month helpers**

```ts
export function normalizeReminderLanguage(
  value: unknown,
): ReminderLanguage {
  return value === "en" ? "en" : "he";
}

export function getMonthlyEncouragement(
  language: ReminderLanguage,
  month: number,
): MonthlyEncouragement {
  if (!Number.isInteger(month) || month < 1 || month > 12) {
    throw new RangeError("Month must be between 1 and 12");
  }

  return REMINDER_COPY[language].monthlyEncouragements[month - 1];
}

export function getIsraelMonth(date = new Date()): number {
  return Number(
    new Intl.DateTimeFormat("en-US", {
      timeZone: "Asia/Jerusalem",
      month: "numeric",
    }).format(date),
  );
}
```

- [ ] **Step 5: Expand tests for Israel month selection**

Add:

```ts
import { getIsraelMonth } from "./email-copy.ts";

it("uses the Israeli calendar month at UTC month boundaries", () => {
  expect(getIsraelMonth(new Date("2026-01-31T22:30:00.000Z"))).toBe(2);
});
```

- [ ] **Step 6: Run focused tests**

Run:

```powershell
npm test -- supabase/functions/send-reminder-emails/email-copy.test.ts
```

Expected: all locale tests pass.

- [ ] **Step 7: Stop for content review**

Review the 24 localized entries and their source labels. Do not commit unless explicitly requested.

---

### Task 3: Redesign the Pure Email Template

**Files:**
- Create: `supabase/functions/send-reminder-emails/email-templates.test.ts`
- Modify: `supabase/functions/send-reminder-emails/email-templates.ts`
- Create: `public/email/reminder-header-blur.png`

**Interfaces:**
- Consumes:
  - `ReminderLanguage`
  - `REMINDER_COPY`
  - `getMonthlyEncouragement(language, month)`
- Produces:
  - `type BalanceState = "outstanding" | "credit" | "settled"`
  - `EmailTemplateData`
  - `getBalanceState(balance)`
  - `extractFirstName(fullName)`
  - `generateReminderEmailSubject(data)`
  - `generateReminderEmailHTML(data)`
  - `generateReminderEmailText(data)`

- [ ] **Step 1: Write failing tests for personalization, status, and localization**

Create `email-templates.test.ts`:

```ts
import { describe, expect, it } from "vitest";
import {
  extractFirstName,
  generateReminderEmailHTML,
  generateReminderEmailSubject,
  generateReminderEmailText,
  getBalanceState,
} from "./email-templates.ts";

const baseData = {
  titheBalance: 384.7,
  maaserBalance: 384.7,
  chomeshBalance: 0,
  language: "he" as const,
  fullName: "יוסי ויינברגר",
  israelMonth: 1,
  unsubscribeUrls: {
    reminderUrl: "https://ten10-app.com/unsubscribe?type=reminder",
    allUrl: "https://ten10-app.com/unsubscribe?type=all",
  },
};

describe("reminder email templates", () => {
  it("classifies all balance states", () => {
    expect(getBalanceState(1)).toBe("outstanding");
    expect(getBalanceState(-1)).toBe("credit");
    expect(getBalanceState(0)).toBe("settled");
  });

  it("extracts only a trimmed first name", () => {
    expect(extractFirstName("  יוסי   ויינברגר ")).toBe("יוסי");
    expect(extractFirstName(null)).toBeUndefined();
    expect(extractFirstName("   ")).toBeUndefined();
  });

  it("renders the approved Hebrew outstanding copy", () => {
    const html = generateReminderEmailHTML(baseData);
    expect(html).toContain('lang="he"');
    expect(html).toContain('dir="rtl"');
    expect(html).toContain("ערב טוב, יוסי");
    expect(html).toContain("יתרת המעשר שלך לתרומה היא");
    expect(html).toContain("384.70");
    expect(html).toContain("כדאי לוודא שהוספת");
    expect(html).toContain("פינת החיזוק");
  });

  it("renders credit without a minus sign", () => {
    const html = generateReminderEmailHTML({
      ...baseData,
      titheBalance: -384.7,
    });
    expect(html).toContain("הינך נמצא בזכות של");
    expect(html).toContain("384.70");
    expect(html).not.toContain("-384.70");
  });

  it("renders the settled state", () => {
    const html = generateReminderEmailHTML({
      ...baseData,
      titheBalance: 0,
    });
    expect(html).toContain("יתרת המעשר שלך מאוזנת");
    expect(html).toContain("0.00");
  });

  it("renders complete English HTML and plain text", () => {
    const data = {
      ...baseData,
      language: "en" as const,
      fullName: "Yossi Weinberger",
    };
    const html = generateReminderEmailHTML(data);
    const text = generateReminderEmailText(data);
    expect(html).toContain('lang="en"');
    expect(html).toContain('dir="ltr"');
    expect(html).toContain("Good evening, Yossi");
    expect(html).toContain("Your remaining tithe balance is");
    expect(text).toContain("Good evening, Yossi");
    expect(text).toContain("Stop monthly reminders");
  });

  it("escapes a user-controlled name", () => {
    const html = generateReminderEmailHTML({
      ...baseData,
      fullName: '<img src=x onerror="alert(1)"> User',
    });
    expect(html).not.toContain("<img src=x");
    expect(html).toContain("&lt;img");
  });

  it("contains the hosted header asset and cream fallback", () => {
    const html = generateReminderEmailHTML(baseData);
    expect(html).toContain(
      "https://ten10-app.com/email/reminder-header-blur.png",
    );
    expect(html).toContain("#f9f6eb");
  });

  it("preserves both unsubscribe actions", () => {
    const html = generateReminderEmailHTML(baseData);
    expect(html).toContain(baseData.unsubscribeUrls.reminderUrl);
    expect(html).toContain(baseData.unsubscribeUrls.allUrl);
    expect(html).toContain("הפסקת תזכורות חודשיות");
    expect(html).toContain("ביטול הרשמה מכל המיילים");
  });

  it("localizes subject lines", () => {
    expect(generateReminderEmailSubject(baseData)).toContain(
      "תזכורת מעשר",
    );
    expect(
      generateReminderEmailSubject({
        ...baseData,
        language: "en",
      }),
    ).toContain("Tithe reminder");
  });
});
```

- [ ] **Step 2: Run tests and verify they fail against the old template**

Run:

```powershell
npm test -- supabase/functions/send-reminder-emails/email-templates.test.ts
```

Expected: failures for missing exports, English localization, new HTML, and plain text.

- [ ] **Step 3: Generate the approved blur asset**

Generate `public/email/reminder-header-blur.png` at 1200x376:

- Warm cream base `#f9f6eb`.
- Soft low-opacity Ten10 teal blur from the upper right.
- Soft low-opacity Ten10 gold blur from the lower left.
- No text, logo, border, glass, noise, or decorative objects.
- Keep the center bright enough for the existing teal logo.
- Export as optimized PNG under 80KB.

Verify:

```powershell
Get-Item "public/email/reminder-header-blur.png" | Select-Object Name, Length
```

Expected: file exists and `Length` is below `81920`.

- [ ] **Step 4: Define pure template data and helpers**

Replace the old boolean flags with:

```ts
import {
  getMonthlyEncouragement,
  REMINDER_COPY,
  ReminderLanguage,
} from "./email-copy.ts";

export type BalanceState = "outstanding" | "credit" | "settled";

export interface EmailTemplateData {
  titheBalance: number;
  maaserBalance?: number;
  chomeshBalance?: number;
  language: ReminderLanguage;
  fullName?: string | null;
  israelMonth: number;
  unsubscribeUrls?: {
    reminderUrl: string;
    allUrl: string;
  };
}

export function getBalanceState(balance: number): BalanceState {
  if (balance > 0) return "outstanding";
  if (balance < 0) return "credit";
  return "settled";
}

export function extractFirstName(
  fullName: string | null | undefined,
): string | undefined {
  const firstName = fullName?.trim().split(/\s+/)[0];
  return firstName || undefined;
}
```

Keep a local `escapeHtml` helper and call it for first names and URLs inserted into HTML.

- [ ] **Step 5: Implement localized amount, subject, HTML, and text**

Use:

```ts
function formatAmount(
  amount: number,
  language: ReminderLanguage,
): string {
  const formatted = new Intl.NumberFormat(
    REMINDER_COPY[language].locale,
    {
      minimumFractionDigits: 2,
      maximumFractionDigits: 2,
    },
  ).format(Math.abs(amount));

  return language === "he" ? `${formatted} ₪` : `₪${formatted}`;
}
```

Generate the greeting with no dangling comma:

```ts
const firstName = extractFirstName(data.fullName);
const greeting = firstName
  ? `${copy.greeting}, ${escapeHtml(firstName)}`
  : copy.greeting;
```

Use email-safe table HTML:

- Outer 100% presentation table with warm background.
- Inner 600px presentation table.
- Header `<td>` with:
  - `background="#f9f6eb"`
  - `background-image:url(https://ten10-app.com/email/reminder-header-blur.png)`
  - centered existing logo
  - 142px gold rule
  - localized slogan
- Centered balance label and amount.
- Centered verification sentence.
- Excel hint.
- Teal CTA.
- Gold divider and localized encouragement below CTA.
- Existing footer links.

Generate plain text from the same copy and data. Do not retain the old hardcoded `Monthly reminder` body.

- [ ] **Step 6: Run focused tests**

Run:

```powershell
npm test -- supabase/functions/send-reminder-emails/email-templates.test.ts
```

Expected: all template tests pass.

- [ ] **Step 7: Inspect one Hebrew and one English HTML fixture**

Add these imports at the top of `email-templates.test.ts`:

```ts
import { mkdirSync, writeFileSync } from "node:fs";
import { resolve } from "node:path";
```

Add this opt-in fixture test:

```ts
if (process.env.WRITE_EMAIL_PREVIEWS === "1") {
  it("writes Hebrew and English preview fixtures", () => {
    const outputDirectory = resolve("tmp/reminder-email-previews");
    mkdirSync(outputDirectory, { recursive: true });

    writeFileSync(
      resolve(outputDirectory, "reminder-he.html"),
      generateReminderEmailHTML(baseData),
      "utf8",
    );
    writeFileSync(
      resolve(outputDirectory, "reminder-en.html"),
      generateReminderEmailHTML({
        ...baseData,
        language: "en",
        fullName: "Yossi Weinberger",
      }),
      "utf8",
    );
  });
}
```

Generate the fixtures:

```powershell
$env:WRITE_EMAIL_PREVIEWS = "1"
npm test -- supabase/functions/send-reminder-emails/email-templates.test.ts
Remove-Item Env:WRITE_EMAIL_PREVIEWS
```

Open:

```text
tmp/reminder-email-previews/reminder-he.html
tmp/reminder-email-previews/reminder-en.html
```

Verify:

- Header remains complete with the background image disabled.
- Mobile width does not overflow.
- Hebrew and English direction are correct.
- Monthly encouragement is below the CTA.

- [ ] **Step 8: Stop for visual review**

Compare generated HTML against the approved visual companion. Do not commit unless explicitly requested.

---

### Task 4: Version and Extend the Reminder Recipient RPC

**Files:**
- Create via Supabase CLI: migration named `localize_reminder_email_context`
- Modify: `supabase/functions/send-reminder-emails/user-service.ts`

**Interfaces:**
- RPC produces:
  - `id uuid`
  - `email varchar`
  - `reminder_enabled boolean`
  - `reminder_day_of_month integer`
  - `full_name text`
  - `language text`
- `ReminderUser` produces:
  - `full_name: string | null`
  - `language: ReminderLanguage`

- [ ] **Step 1: Discover the migration command**

Run:

```powershell
npx supabase@latest migration new --help
```

Expected: help text for creating a local migration.

- [ ] **Step 2: Generate the migration file**

Run:

```powershell
npx supabase@latest migration new localize_reminder_email_context
```

Expected: Supabase prints the exact generated file path. Use that path for the rest of this task. Do not hand-create or rename the timestamp.

- [ ] **Step 3: Add the canonical RPC definition**

Write this SQL into the generated migration:

```sql
begin;

drop function if exists public.get_reminder_users_with_emails(integer);

create function public.get_reminder_users_with_emails(reminder_day integer)
returns table (
  id uuid,
  email varchar,
  reminder_enabled boolean,
  reminder_day_of_month integer,
  full_name text,
  language text
)
language plpgsql
security definer
set search_path = public
as $function$
begin
  return query
  select
    p.id,
    u.email::varchar,
    p.reminder_enabled,
    p.reminder_day_of_month,
    p.full_name,
    case
      when p.client_preferences->>'language' = 'en' then 'en'
      else 'he'
    end::text as language
  from public.profiles p
  join auth.users u on p.id = u.id
  where p.reminder_enabled = true
    and p.reminder_day_of_month = reminder_day
    and coalesce(p.mailing_list_consent, false) = true
    and u.email is not null;
end;
$function$;

comment on function public.get_reminder_users_with_emails(integer) is
  'Returns consented reminder recipients with normalized language and profile name; callable by service_role only.';

revoke all on function public.get_reminder_users_with_emails(integer)
  from public, anon, authenticated;
grant execute on function public.get_reminder_users_with_emails(integer)
  to service_role;

do $$
begin
  if has_function_privilege(
    'anon',
    'public.get_reminder_users_with_emails(integer)',
    'execute'
  ) then
    raise exception 'anon still has EXECUTE on get_reminder_users_with_emails';
  end if;

  if has_function_privilege(
    'authenticated',
    'public.get_reminder_users_with_emails(integer)',
    'execute'
  ) then
    raise exception 'authenticated still has EXECUTE on get_reminder_users_with_emails';
  end if;

  if not has_function_privilege(
    'service_role',
    'public.get_reminder_users_with_emails(integer)',
    'execute'
  ) then
    raise exception 'service_role lacks EXECUTE on get_reminder_users_with_emails';
  end if;
end;
$$;

commit;
```

- [ ] **Step 4: Update TypeScript recipient interfaces**

At the top of `user-service.ts`, add:

```ts
import { ReminderLanguage } from "./email-copy.ts";
```

Update:

```ts
export interface ReminderUser {
  id: string;
  email: string;
  reminder_enabled: boolean;
  reminder_day_of_month: number;
  full_name: string | null;
  language: ReminderLanguage;
}
```

The existing object spread in `getUsersWithTitheBalances` then carries both fields without extra queries.

- [ ] **Step 5: Validate migration syntax locally**

First discover available commands:

```powershell
npx supabase@latest db lint --help
```

If the local stack is running:

```powershell
npx supabase@latest db lint --local
```

Expected: no migration SQL errors.

If the local stack is not running, stop and report that validation as pending. Do not apply this migration remotely as a workaround.

- [ ] **Step 6: Review security invariants**

Confirm in the SQL diff:

- `security definer`
- fixed `search_path`
- consent filter
- non-null email filter
- anon and authenticated revoked
- service role granted
- no user metadata used for authorization

- [ ] **Step 7: Stop for review**

Do not apply remotely and do not commit unless explicitly requested.

---

### Task 5: Pass Locale and Name Through SES Delivery

**Files:**
- Modify: `supabase/functions/send-reminder-emails/simple-email-service.ts`
- Test: `supabase/functions/send-reminder-emails/email-templates.test.ts`

**Interfaces:**
- Consumes:
  - `generateReminderEmailSubject(data)`
  - `generateReminderEmailHTML(data)`
  - `generateReminderEmailText(data)`
  - `getIsraelMonth(date)`
  - recipient `full_name`
  - recipient `language`
- Produces:
  - localized multipart HTML and text.

- [ ] **Step 1: Extend service method inputs**

Update imports:

```ts
import {
  generateReminderEmailHTML,
  generateReminderEmailSubject,
  generateReminderEmailText,
  EmailTemplateData,
} from "./email-templates.ts";
import {
  getIsraelMonth,
  ReminderLanguage,
} from "./email-copy.ts";
```

Update `sendReminderEmail`:

```ts
async sendReminderEmail(
  userEmail: string,
  userId: string,
  titheBalance: number,
  maaserBalance: number | undefined,
  chomeshBalance: number | undefined,
  language: ReminderLanguage,
  fullName: string | null,
): Promise<EmailResult>
```

- [ ] **Step 2: Build one complete template data object**

```ts
const templateData: EmailTemplateData = {
  titheBalance,
  maaserBalance,
  chomeshBalance,
  language,
  fullName,
  israelMonth: getIsraelMonth(),
  unsubscribeUrls,
};

const subject = generateReminderEmailSubject(templateData);
const htmlBody = generateReminderEmailHTML(templateData);
const textBody = generateReminderEmailText(templateData);
```

Delete the old hardcoded English `textBody`.

- [ ] **Step 3: Extend the bulk recipient shape**

```ts
async sendBulkReminders(
  users: Array<{
    id: string;
    email: string;
    titheBalance: number;
    maaserBalance?: number;
    chomeshBalance?: number;
    language: ReminderLanguage;
    full_name: string | null;
  }>,
): Promise<EmailResult[]>
```

Pass:

```ts
u.language,
u.full_name,
```

after the balance arguments.

- [ ] **Step 4: Keep MIME and unsubscribe behavior unchanged**

Verify the diff does not alter:

- `List-Unsubscribe`
- `List-Unsubscribe-Post`
- Raw MIME boundaries
- UTF-8 base64 encoding
- SES configuration set
- hashed user tag
- 100ms bulk delay

- [ ] **Step 5: Run all focused email tests**

Run:

```powershell
npm test -- supabase/functions/send-reminder-emails
```

Expected: copy and template tests pass.

- [ ] **Step 6: Run Supabase import validation**

Run:

```powershell
npm run check-supabase-imports
```

Expected: exit code 0.

- [ ] **Step 7: Stop for integration review**

Do not send real email and do not commit unless explicitly requested.

---

### Task 6: Update Email Documentation

**Files:**
- Modify: `supabase/functions/send-reminder-emails/README.md`
- Modify: `llm-instructions/features/email/email-reminders-feature-complete-guide.md`
- Modify: `llm-instructions/features/email/email-system-future-improvements.md`
- Modify: `llm-instructions/backend/supabase-integration-status.md`

**Interfaces:**
- Consumes: final implementation and migration contract.
- Produces: accurate maintenance, testing, and deployment guidance.

- [ ] **Step 1: Correct the function README**

Make these exact corrections:

- Replace `email-service.ts` with `simple-email-service.ts`.
- Replace reminder days `1, 7, 10, 15, 20` with `1, 5, 10, 15, 20, 25`.
- Remove the incorrect statement that negative balances are red.
- Document Hebrew and English locale selection from `client_preferences.language`.
- Document Hebrew fallback.
- Document `full_name` first-name greeting.
- Document `email-copy.ts` and the 12 monthly encouragement entries.
- Document the header asset URL and cream fallback.
- Replace anon-key test instructions with service-role-only test-mode guidance.

- [ ] **Step 2: Update the complete feature guide**

Replace stale code samples with current architecture:

```text
index.ts
user-service.ts
email-copy.ts
email-templates.ts
simple-email-service.ts
jwt-utils.ts
```

Document the RPC fields:

```text
id, email, reminder_enabled, reminder_day_of_month,
full_name, language
```

Record that subject, HTML, and plain text are all localized.

- [ ] **Step 3: Update future improvements**

Remove or mark complete:

- New `preferred_language` column.
- Basic multilingual reminder templates.
- List-Unsubscribe implementation.
- Basic responsive redesign.

Keep genuinely future items such as analytics and broader transactional-email localization.

- [ ] **Step 4: Update integration status**

Add one concise bullet stating:

```text
Reminder emails use profiles.client_preferences.language, default to Hebrew,
include an optional first-name greeting, and rotate 12 localized encouragement
items by the current month in Asia/Jerusalem.
```

- [ ] **Step 5: Search for stale claims**

Run:

```powershell
rg "email-service\.ts|1, 7, 10, 15, 20|preferred_language|List-Unsubscribe.*not implemented" `
  "supabase/functions/send-reminder-emails" `
  "llm-instructions/features/email" `
  "llm-instructions/backend/supabase-integration-status.md"
```

Expected: no stale implementation claims remain.

- [ ] **Step 6: Stop for documentation review**

Do not commit unless explicitly requested.

---

### Task 7: Full Local Verification and Deployment Handoff

**Files:**
- Verify all files changed by Tasks 1 through 6.

**Interfaces:**
- Produces: local evidence that the feature is ready for staging.

- [ ] **Step 1: Run focused tests**

```powershell
npm test -- supabase/functions/send-reminder-emails
```

Expected: all reminder copy and template tests pass.

- [ ] **Step 2: Run the full test suite**

```powershell
npm test
```

Expected: all tests pass.

- [ ] **Step 3: Run lint**

```powershell
npm run lint
```

Expected: exit code 0 with no new warnings in changed files.

- [ ] **Step 4: Run the production build**

```powershell
npm run build
```

Expected: Vite production build completes successfully.

- [ ] **Step 5: Run Supabase import checks**

```powershell
npm run check-supabase-imports
```

Expected: exit code 0.

- [ ] **Step 6: Inspect changed files**

```powershell
git status --short
git diff --check
git diff -- `
  "vitest.config.ts" `
  "public/email/reminder-header-blur.png" `
  "supabase/functions/send-reminder-emails" `
  "supabase/migrations" `
  "llm-instructions/features/email" `
  "llm-instructions/backend/supabase-integration-status.md"
```

Expected:

- No whitespace errors.
- No unrelated changes.
- No secrets.
- No `.superpowers/brainstorm` files included.

- [ ] **Step 7: Prepare staging SQL verification**

After explicit approval to apply the migration in staging, verify:

```sql
select
  language,
  count(*)
from public.get_reminder_users_with_emails(10)
group by language
order by language;
```

Then verify privileges:

```sql
select
  has_function_privilege(
    'anon',
    'public.get_reminder_users_with_emails(integer)',
    'execute'
  ) as anon_execute,
  has_function_privilege(
    'authenticated',
    'public.get_reminder_users_with_emails(integer)',
    'execute'
  ) as authenticated_execute,
  has_function_privilege(
    'service_role',
    'public.get_reminder_users_with_emails(integer)',
    'execute'
  ) as service_role_execute;
```

Expected:

```text
anon_execute = false
authenticated_execute = false
service_role_execute = true
```

- [ ] **Step 8: Prepare controlled email verification**

After explicit approval to deploy to staging:

1. Send one Hebrew outstanding-balance email.
2. Send one Hebrew credit email.
3. Send one English email.
4. Verify subject, logo, blur background, fallback, CTA, monthly content, plain text, and both unsubscribe links.
5. Verify Gmail web and mobile.
6. Verify Outlook web.
7. Verify Outlook desktop if available.

- [ ] **Step 9: Report readiness**

Return:

- Changed-file summary.
- Test, lint, build, and import-check results.
- Migration validation status.
- Manual client checks completed and pending.
- Explicitly note that production migration and deployment remain unperformed unless separately approved.

Do not commit, push, apply a remote migration, or deploy unless explicitly requested.

---

## Plan Self-Review

- Spec coverage: visual design, languages, first name, three balance states, Excel hint, monthly content, source labels, plain text, RPC, security, compatibility, docs, tests, and rollout are each mapped to tasks.
- Completeness scan found no unresolved markers or unnamed error-handling steps.
- Type consistency:
  - `ReminderLanguage` is defined once in `email-copy.ts`.
  - `EmailTemplateData` uses `language`, `fullName`, and `israelMonth`.
  - RPC `full_name` maps to service argument `fullName`.
  - Monthly selection consistently uses month numbers 1 through 12.
  - Subject, HTML, and text consume the same `EmailTemplateData`.
- Scope: one cohesive reminder-email feature. No unrelated email types are modified.
